<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="300dp"
    android:layout_height="200dp"
    android:layout_gravity="center"
    xmlns:android="http://schemas.android.com/apk/res/android" >
<TextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/title_dialog"
    android:gravity="center"
    android:textSize="15sp"
    android:id="@+id/tvDialogCreateNewPlaylist"
    />
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:maxLines="1"
        android:id="@+id/edtNamePlaylist"
        android:layout_below="@+id/tvDialogCreateNewPlaylist"
        android:hint="@string/input_playlist"
        android:textSize="20sp"
        android:inputType="text" />
</RelativeLayout>
